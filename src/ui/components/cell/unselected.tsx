import * as React from "react";
import { observer } from "mobx-react-lite";

export const UnselectedCell = observer(() => {
    return (
        <>
            Select a cell on the map
        </>
    );
});